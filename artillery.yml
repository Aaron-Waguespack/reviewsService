config:
  target: "http://127.0.0.1:3020/reviews"
  phases:
    - duration: 10
      arrivalRate: 500

  processor: "./artfun.js"
scenarios:
  - flow:
      - function: "setId"
      - get:
          url: "/{{ id }}/metas"
